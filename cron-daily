#!/bin/sh

cd $HOME/src/trick_skel
if [ $? -ne 0 ]; then
    exit
fi

TRICKV_HOSTNAME=`hostname -s`

case $TRICKV_HOSTNAME in
    bigweld|sting|mc|sam) svn up > /dev/null;;
esac

if [ $TRICKV_HOSTNAME = "bigweld" ]; then
    ./mintel_push.sh > /dev/null
fi

#./install.sh > /dev/null
