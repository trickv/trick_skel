#!/bin/sh
# ``vacuum'' from trick_skel

if [ -f Makefile ]; then
	make -k maintainer-clean > /dev/null
fi

rm -rf compile INSTALL config.h.in aclocal.m4 ltmain.sh Makefile.in depcomp missing install-sh configure config.sub config.guess mkinstalldirs *.pidb autom4te.cache *.userprefs

