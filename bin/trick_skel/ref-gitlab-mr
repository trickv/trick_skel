#!/usr/bin/env bash

cat <<EOF

To push and create a Gitlab MR:

gitlab-push-mr OR
    git push --set-upstream origin $(git branch --show-current) -o merge_request.create

See more: https://docs.gitlab.com/ee/user/project/push_options.html#push-options-for-merge-requests



EOF
