add ~/.ssh/config files for each hostname

case on bzip2's --show-progress flag should be based on an install-time check against bzip2's version.  sting has bzip2 1.0.3 which does not work; only 1.0.4+ will work
